#!/bin/sh

CONFIG_HOSTS=127.0.0.1 RAILS_ENV=test ./bin/rails s -u puma -b 127.0.0.1 -p 3000 &
numpid="$!"
echo "Proceso $numpid"

sleep 10 && yarn test
ret="$?"

echo "Matando proceso $numpid"
kill $numpid

return $ret
