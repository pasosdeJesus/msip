<% cnus = Ability::ROLES.clone.select { |v| 
  v.last != 0 } %>
<%= f.input :busrol,
  collection: cnus,
  include_blank: true,
  input_html: {
    class: 'tom-select',
    'data-action': 'change->msip--enviarautomatico#enviar'
  },
  label: false,
  selected: Msip::ModeloHelper.poromision(params, :busrol)
%>
