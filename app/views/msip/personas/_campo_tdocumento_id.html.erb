<%= render(partial: 'msip/modelos/msip_campo_aut',
  locals: {
    f: f, 
    atr: 'tdocumento_id', 
    atributos_internos: {
      'data-action': 'change->msip--sindocaut#cambia_tdocumento'
    },
    opciones: {
      chosen: false 
    }
  })
  # No usamox chosen porque usa eventos jQuery y no eventos estándar.
  # Tal vez funcione convertir eventos jQuery a eventos estándar con
  # https://www.npmjs.com/package/jquery-events-to-dom-events
  # Pero sería mejor cambiar a algo que no dependa de jQuery
%>
